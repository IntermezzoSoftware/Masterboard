export function ResultBadge({ result }: { result: string }) {
  return <span className="font-medium text-[var(--color-content-secondary)] dark:text-[var(--color-dark-content-secondary)]">{result}</span>
}
